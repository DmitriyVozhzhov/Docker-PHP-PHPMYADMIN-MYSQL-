This is simple settings to Docker compose with /php8-apache, phpmyadmin, mysql/

Create this directories structure:

├── docker-compose.yml
├── Dockerfile
├── dump
│    └── myDb.sql
├── sessions
└── src
     └── index.php

    